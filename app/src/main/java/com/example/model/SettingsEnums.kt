package com.example.model

enum class DesktopTransitionEffect { NONE, ACCORDION, CUBE, FADE, ZOOM }
enum class DockBackgroundStyle { TRANSPARENT, BLURRED, GLASS, SOLID_CARD }
enum class DrawerSortOrder { ALPHABETICAL, INSTALL_DATE, MOST_USED, COLOR }
enum class NotificationBadgeStyle { NONE, DOT, NUMBER }
enum class DarkThemeMode { SYSTEM, ALWAYS_DARK, ALWAYS_LIGHT }
enum class SearchEngineProvider { GOOGLE, DUCKDUCKGO, BING, LOCAL_ONLY }
enum class SearchTarget { APPS, WEB, CONTACTS, DEVICE_SHORTCUTS }
